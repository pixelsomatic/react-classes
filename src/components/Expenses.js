import React, { Component } from 'react'
import { Container, AddButton, Buttons } from './../styles/expenses'

class List extends Component {

  render() {
    return (
      <Container>
        testesss
        <Buttons>
          <AddButton>ADD</AddButton>
        </Buttons>
      </Container>
    )
  }
}

export default List